<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E94250">

    <LinearLayout
        android:id="@+id/ll_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/bg_login"
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_marginRight="40dp">

        <EditText
            android:id="@+id/et_username"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/colorAccent"
            android:hint="@string/username_hint"
            android:inputType="text"
            android:minHeight="48dp"
            android:textColorHint="@color/colorHint" />

        <EditText
            android:id="@+id/et_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/colorAccent"
            android:hint="@string/password_hint"
            android:inputType="textPassword"
            android:minHeight="48dp"
            android:textColorHint="@color/colorHint"
            />

        <Button
            android:id="@+id/btn_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/ecn_green"
            android:text="@string/login"
            android:textColor="@color/colorButtonText" />

        <Spinner
            android:id="@+id/spn_language"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:entries="@array/language_list"
            android:layout_marginTop="16dp"/>

        <Button
            android:id="@+id/btn_reset_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/ecn_color"
            android:text="@string/reset_password"
            android:layout_marginTop="16dp"
            android:textColor="@color/colorButtonText" />




    </LinearLayout>


    <ImageView
        android:id="@+id/logo_ecn"
        android:layout_width="wrap_content"
        android:layout_height="195dp"
        android:layout_alignTop="@+id/ll_login"
        android:layout_alignBottom="@+id/ll_login"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="63dp"
        android:layout_marginTop="-211dp"
        android:layout_marginEnd="65dp"
        android:layout_marginBottom="296dp"
        android:background="#E94250"
        android:src="@drawable/logo_ecn" />


</RelativeLayout>
